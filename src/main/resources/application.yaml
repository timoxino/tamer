spring:
  ai:
    openai:
      chat:
        options:
          model: gpt-4o
          temperature: 1.0
      api-key: test
  cloud:
    gcp:
      core:
        enabled: false
      pubsub:
        enabled: false
      storage:
        enabled: false
server:
  error:
    include-message: always
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always